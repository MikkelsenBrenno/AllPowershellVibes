# Device Configuration

Custom compliance discovery scripts and JSON rules for Windows configuration state, registry-backed settings, power settings, time settings, and operating system behavior.

Use this category when configuration state should be evaluated as compliant or noncompliant.

## Folder Pattern

```text
Custom-Compliance/Device-Configuration/<Script-Name>/
```
