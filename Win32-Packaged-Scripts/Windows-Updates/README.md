# Windows Updates

Win32 packaged scripts for Windows Update tooling, delivery optimization packages, reboot helper packages, and update-related installable changes.

Use this category when update-related logic needs install, uninstall, and detection.

## Folder Pattern

```text
Win32-Packaged-Scripts/Windows-Updates/<Script-Name>/
```
