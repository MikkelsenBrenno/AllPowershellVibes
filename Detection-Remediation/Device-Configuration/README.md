# Device Configuration

Detection and remediation packages for Windows device configuration, registry-backed settings, power settings, time settings, and operating system behavior.

Use this category when a device-level configuration should be kept in a known state.

## Folder Pattern

```text
Detection-Remediation/Device-Configuration/<Script-Name>/
```
