# Browser-Management

Detection and Remediation packages for browser policy, extension control, profile health, update readiness, cache state, and browser support workflows.

Use this category when technicians need scripts that are easier to find by browser or remote-work scenario rather than a broader application, networking, or user-experience bucket.

## Folder Pattern

``text
Detection-Remediation/Browser-Management/<Script-Name>/
``
